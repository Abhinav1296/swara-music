# API base path. Left empty so the app uses the Vite dev proxy ("/api").
# For a production build pointing at a remote backend, set e.g.:
# VITE_API_BASE=https://your-backend.example.com
VITE_API_BASE=
